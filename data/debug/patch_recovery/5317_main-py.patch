*** Begin Patch
*** Update File: C:\Users\Dokas\Desktop\f1-telemetry-analyzer\backend\main.py
@@
         "visualGeometryEnabled": bool(track.get("visualGeometry")),
         "visualGeometrySource": (track.get("visualGeometry") or {}).get("source") or (track.get("metadata") or {}).get("visualGeometrySource"),
+        "visualSource": (track.get("visualGeometry") or {}).get("visualSource") or (track.get("metadata") or {}).get("visualSource"),
+        "visualArtifactFixEnabled": (track.get("visualGeometry") or {}).get("visualArtifactFixEnabled") or (track.get("metadata") or {}).get("visualArtifactFixEnabled"),
+        "falseCurveArtifactsRemoved": (track.get("visualGeometry") or {}).get("falseCurveArtifactsRemoved") or (track.get("metadata") or {}).get("falseCurveArtifactsRemoved"),
         "visualWidthMin": (track.get("visualGeometry") or {}).get("widthMin"),
         "visualWidthAvg": (track.get("visualGeometry") or {}).get("widthAvg"),
         "visualWidthMax": (track.get("visualGeometry") or {}).get("widthMax"),
         **status,
     }
@@
         "source": visual.get("source"),
+        "visualSource": visual.get("visualSource") or (visual.get("metadata") or {}).get("visualSource"),
         "method": visual.get("method"),
+        "visualArtifactFixEnabled": visual.get("visualArtifactFixEnabled"),
+        "falseCurveArtifactsRemoved": visual.get("falseCurveArtifactsRemoved"),
         "visualPointCount": len(center.get("x", [])),
         "widthMin": visual.get("widthMin"),
         "widthAvg": visual.get("widthAvg"),
         "widthMax": visual.get("widthMax"),
         "removedSpikeCount": visual.get("removedSpikeCount"),
@@
             "previewSvg": str(DEBUG_DIR / "track_visual_geometry_preview.svg"),
             "physicsVsVisualSvg": str(DEBUG_DIR / "track_physics_vs_visual_geometry.svg"),
             "artifactsSvg": str(DEBUG_DIR / "track_visual_edge_artifacts.svg"),
             "widthProfileJson": str(DEBUG_DIR / "track_visual_width_profile.json"),
             "artifactsRemovedJson": str(DEBUG_DIR / "track_visual_artifacts_removed.json"),
+            "falseCurveReportJson": str(DEBUG_DIR / "track_visual_false_curves_report.json"),
+            "roadOnlyPreviewSvg": str(DEBUG_DIR / "track_visual_road_only_preview.svg"),
+            "fixedFalseCurvesSvg": str(DEBUG_DIR / "track_visual_fixed_false_curves.svg"),
+            "physicsVsVisualAfterFalseCurveFixSvg": str(DEBUG_DIR / "track_physics_vs_visual_after_false_curve_fix.svg"),
         },
     }
*** End Patch
