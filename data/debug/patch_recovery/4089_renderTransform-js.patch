*** Begin Patch
*** Update File: C:\Users\Dokas\Desktop\f1-telemetry-analyzer\frontend\src\components\map\renderTransform.js
@@
 export function toRenderPoint(point, options = {}) {
   if (!point) return null;
   const x = Number(point.x);
   const y = Number(point.y);
   if (!Number.isFinite(x) || !Number.isFinite(y)) return null;
-  return {
-    x: options.mirrorX ? -x : x,
-    y,
-  };
+  return { x, y };
 }
@@
 export function toRenderVector(vector, options = {}) {
   if (!vector) return null;
   const x = Number(vector.x);
   const y = Number(vector.y);
   if (!Number.isFinite(x) || !Number.isFinite(y)) return null;
-  return {
-    x: options.mirrorX ? -x : x,
-    y,
-  };
+  return { x, y };
 }
 
 export function toRenderHeading(heading = 0, options = {}) {
-  const value = Number.isFinite(heading) ? heading : 0;
-  return options.mirrorX ? -value : value;
+  return Number.isFinite(heading) ? heading : 0;
 }
 
 export function toRenderBounds(bounds, options = {}) {
-  if (!bounds) return bounds;
-  if (!options.mirrorX) return bounds;
-  const minX = Number(bounds.minX);
-  const maxX = Number(bounds.maxX);
-  if (!Number.isFinite(minX) || !Number.isFinite(maxX)) return bounds;
-  const mirroredMinX = -maxX;
-  const mirroredMaxX = -minX;
-  return {
-    ...bounds,
-    minX: mirroredMinX,
-    maxX: mirroredMaxX,
-    cx: (mirroredMinX + mirroredMaxX) / 2,
-    w: Math.max(mirroredMaxX - mirroredMinX, 1),
-  };
+  return bounds;
+}
+
+export function mirrorMode(options = {}) {
+  return options.mirrorX ? 'screen-space' : 'off';
 }
*** End Patch
